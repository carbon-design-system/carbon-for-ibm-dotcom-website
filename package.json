{
  "name": "gatsby-starter-carbon-theme",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "dev:clean": "yarn clean && yarn dev",
    "build": "gatsby build --prefix-paths",
    "build:clean": "yarn clean && gatsby build --prefix-paths",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "build:analyze": "ANALYZE=true yarn build"
  },
  "dependencies": {
    "carbon-components-react": "^7.20.0",
    "dotenv": "^8.2.0",
    "gatsby": "^2.19.0",
    "gatsby-theme-carbon": "^1.26.11",
    "prop-types": "^15.7.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
