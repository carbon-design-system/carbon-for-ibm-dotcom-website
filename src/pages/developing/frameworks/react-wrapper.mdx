---
title: Frameworks
description: Carbon for IBM.com Web Components, using Custom Elements v1 and Shadow DOM v1 specs.
tabs: ['Web Components', 'React wrapper']
---

import { Link } from 'gatsby';

<PageDescription>

Our team builds new components for Carbon for IBM.com as web components first. All components are provided as web components with a React
wrapper for seamless integration into a React application.

</PageDescription>

<AnchorLinks>
  <AnchorLink>Why React wrappers?</AnchorLink>
  <AnchorLink>Install via import</AnchorLink>
  <AnchorLink>Getting started</AnchorLink>
  <AnchorLink>NextJS template</AnchorLink>
  <AnchorLink>Available components</AnchorLink>
</AnchorLinks>

## Why React wrappers?

When using web components, other frameworks such as Angular and Vue require limited to no changes in the application, but React has some issues. React heavily relies on its knowledge with regard to how intrinsic HTML elements work. In order to solve these issues,
our team created the React wrapper components.

## Install via import

Using [npm](https://www.npmjs.com/):

```bash
npm install -S @carbon/ibmdotcom-web-components
```

Using [Yarn](https://yarnpkg.com/en/):

```bash
yarn add @carbon/ibmdotcom-web-components
```

### lit

`@carbon/ibmdotcom-web-components` uses [`lit`](https://lit.dev/) for reactive templating on
top of the Web Components standard.

## Getting started

1. These components require the use of [Webpack](https://webpack.js.org/) in
   your project. See our
   [`webpack.config.js`](https://github.com/carbon-design-system/carbon-for-ibm-dotcom/blob/main/packages/web-components/examples/codesandbox/components-react/button-group/webpack.config.js)
   for a simple example configuration.
2. Since these components are being generated from the Carbon for IBM.com Web Component library, the components will import all of the styles themselves.

## NextJS template

To build a light static site, the
[NextJS test](https://github.com/carbon-design-system/carbon-for-ibm-dotcom-nextjs-test) page
is available which includes all components with React wrappers.

## Available components

<Row className="resource-card-group">

<Column colMd={4} colLg={4} noGutterSm>
<ResourceCard
  subTitle="Carbon for IBM.com React wrapper Storybook"
  href="https://www.ibm.com/standards/carbon/web-components/react/"
  target="_blank"
>

![Storybook React wrapper](./../../../images/icon/react-icon.svg)

</ResourceCard>
</Column>
</Row>
